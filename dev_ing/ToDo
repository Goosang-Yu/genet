GenET = Genome Editing Toolkit

fastq file에서 barcode sorting, read counting 하는 code -> mp
gRNA design 해주는것
Saturation library design 해주는것
silent mutation 만드는것
결과를 그래프로 그려주는 것
Deep learning model -> 각종 gRNA에 대해서 예측값내주기

DeepPrime 모델 예측 함수 구현하기

v 0.0.5
1. setup에서 pandas 자동 설치가 되도록 추가
2. ViennaRNA 설치에서 문제가 있음. 우선 설치 순서를 변경해봤는데, 추후 확인 필요
3. DeepPrime 다른 모델들 추가로 넣기

v 0.0.6
1. setup.py에서 install_require 문제가 있던 내용 수정