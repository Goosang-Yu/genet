GenET = Genome Editing Toolkit

fastq file에서 barcode sorting, read counting 하는 code -> mp
gRNA design 해주는것
Saturation library design 해주는것
silent mutation 만드는것
결과를 그래프로 그려주는 것
Deep learning model -> 각종 gRNA에 대해서 예측값내주기

DeepPrime 모델 예측 함수 구현하기

To v 0.0.4
1. 환경 구축 과정에서 protobuf 버전 때문에 tensorflow import 문제가 있던 내용 해결
-> 모든 설치가 끝난 후 protobuf==3.20.*으로 다운그레이드 해주었더니 tf import 문제 없이 해결

2. model file들의 경로 (.pt 등)를 정확히 불러와야 함.
